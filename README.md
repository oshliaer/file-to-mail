# Сервис по отправке файлов на почту

## Заметки по учебному плану

2026-09-07 13:07:42 sinatra не устанавливает автоматически puma и rackup, поэтому их нужно добавить в Gemfile.

2026-09-07 13:08:10 require 'bundler/setup' -- хороший тон

2026-09-07 13:08:29 Начинается с Gemfile

2026-09-07 14:06:45 Для работы с env-файлами используется гем dotenv. Он позволяет загружать переменные окружения из файла .env в процесс Ruby.

2026-09-07 14:25:59 Правильная настройка SMTP для Mail.ru в Ruby:

```ruby
SMTP_OPTIONS = {
  address:              ENV['SMTP_ADDRESS'],     # 'smtp.mail.ru'
  port:                 ENV['SMTP_PORT'].to_i,   # 465
  user_name:            ENV['SMTP_USER_NAME'],
  password:             ENV['SMTP_PASSWORD'],
  authentication:       :plain,                  # В Ruby лучше использовать символ :plain, а не строку 'plain'
  
  # Вот эта связка лечит таймаут на Mail.ru:
  ssl:                  true,                    # Включаем жесткий SSL для порта 465
  tls:                  true,                    # Дублируем для совместимости с net/smtp
  enable_starttls:      false,                   # Намертво КАТЕГОРИЧЕСКИ ОТКЛЮЧАЕМ starttls
  enable_starttls_auto: false                    # Отключаем авто-попытки включить starttls
}
```

2026-09-07 14:29:27 С форматерами в vscode не понятно. Напрмиер, какой-то форматер меняет `unless ... return` на  гвард `return ... unless`.

2026-09-07 14:30:38 Использовать символы везде, где есть вопросы или сомнения. `authentication:       :plain`

2026-09-07 14:31:27 Локальные переменные. Например, `params` в sinatra - это локальная переменная, которая доступна в любом месте внутри блока маршрута. Она не является глобальной переменной и не может быть использована вне этого блока, например, в блоке `begin` (что это вообще?).
