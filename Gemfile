source 'https://rubygems.org'

gem 'sinatra'
gem 'mail'
gem 'rackup'
gem 'puma'
gem 'dotenv'

group :development do
  # Форматирование (.rubocop.yml, автоматически при сохранении)
  # и линт (.rubocop_lint.yml, вручную через bin/lint).
  gem 'rubocop', require: false
end
